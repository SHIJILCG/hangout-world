# Local development uses ws://<hostname>:2567 by default. WebRTC STUN/TURN
# settings are served privately by the game server, never bundled as VITE_ vars.
# Set this to wss://<your-render-service>.onrender.com for a production build.
VITE_SERVER_URL=
